package Lab3.src.main.java.refresher;

import snapshot.Snapshot;

public class RefresherJob {
  public void run(Snapshot snapshot) {
    // TODO: basically should schedule StatusCommand.execute() every 5 seconds.
  }
}
